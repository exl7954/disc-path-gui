import './SampleInputs.css';
import { useState } from 'react';

export default function SampleInputs({ setExternalChange }) {
    const [isOpen, setIsOpen] = useState(false);

    return (
        <>
        
        </>
    )
}